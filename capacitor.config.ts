import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.abelektronik.inventory',
  appName: 'ABElektronik Inventory',
  webDir: 'dist'
};

export default config;
